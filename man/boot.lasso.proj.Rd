% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot_lasso_proj.R
\name{boot.lasso.proj}
\alias{boot.lasso.proj}
\title{Bootstrapped de-sparsified lasso: p-values and confidence intervals}
\usage{
boot.lasso.proj(
  x,
  y,
  family = "gaussian",
  standardize = TRUE,
  multiplecorr.method = "WY",
  parallel = FALSE,
  ncores = getOption("mc.cores", 2L),
  betainit = "cv lasso",
  sigma = NULL,
  Z = NULL,
  verbose = FALSE,
  return.Z = FALSE,
  robust = FALSE,
  B = 1000,
  boot.shortcut = FALSE,
  return.bootdist = FALSE,
  wild = FALSE,
  gaussian.stub = FALSE,
  boot.type = if (wild) "wild" else "residual",
  multiplier = c("gaussian", "mammen"),
  boot.H0c = identical(multiplecorr.method, "WY"),
  groups = NULL
)
}
\arguments{
\item{x}{Design matrix (n x p), without intercept column.}

\item{y}{Response vector of length n.}

\item{family}{Only \code{"gaussian"} is supported.}

\item{standardize}{Should the columns of \code{x} be scaled to unit variance
(they are always centred)?}

\item{multiplecorr.method}{\code{"WY"} (default; Westfall-Young max-T procedure
based on the bootstrap under the complete null hypothesis) or any method
of \code{\link[stats:p.adjust]{stats::p.adjust()}}.}

\item{parallel, ncores}{Compute the nodewise regressions in parallel
(forking; not on Windows). Does not change the results.}

\item{betainit}{\code{"cv lasso"} (default) or \code{"scaled lasso"}.}

\item{sigma}{Optional noise standard deviation, overriding the estimate.}

\item{Z}{Optional matrix of nodewise residuals (e.g. from a previous call
with \code{return.Z = TRUE}) to skip the nodewise lasso. This also skips the
random draw of its cross-validation folds.}

\item{verbose}{Print progress information.}

\item{return.Z}{Return the nodewise residuals Z.}

\item{robust}{Use the robust (sandwich) standard errors of Dezeure,
Bühlmann and Zhang (2017), valid under heteroscedastic errors.}

\item{B}{Number of bootstrap samples.}

\item{boot.shortcut}{Refit the bootstrap lasso at the lambda of the
original fit instead of re-tuning it (faster).}

\item{return.bootdist}{Return the bootstrap distributions (needed for
\link[=confint.silm_proj]{confint()}).}

\item{wild}{Use the wild bootstrap with Gaussian multipliers.}

\item{gaussian.stub}{Developer option of hdi: replace the bootstrap
distribution by independent standard normal draws.}

\item{boot.type}{\code{"residual"}, \code{"wild"} or \code{"xyz"} (xyz-paired bootstrap).
Defaults to \code{"wild"} if \code{wild = TRUE}, else \code{"residual"}.}

\item{multiplier}{Multipliers of the wild bootstrap: \code{"gaussian"} (default,
as in hdi) or \code{"mammen"}.}

\item{boot.H0c}{Also compute the bootstrap under the complete null
hypothesis (always done, and required, for \code{multiplecorr.method = "WY"});
needed for \code{\link[=groupTest]{groupTest()}}.}

\item{groups}{Optional group (vector of indices or names) or list of groups
for which bootstrap summaries are stored, so that simultaneous intervals
and group tests for them are available without \code{return.bootdist = TRUE}.}
}
\value{
An object of class \code{c("silm_boot_lasso_proj", "silm_proj")}: a list
with the elements of hdi's result, in the same order, \code{pval},
\code{pval.corr}, \code{sigmahat}, \code{standardize}, \code{sds}, \code{bhat}, \code{se}, \code{betahat},
\code{family}, \code{method} (\code{"boot.lasso.proj"}), \code{B}, \code{boot.shortcut}, \code{lambda},
\code{call}, \code{Z} (if \code{return.Z = TRUE}), \code{cboot.dist} and
\code{cboot.dist.underH0c} (if \code{return.bootdist = TRUE}: the bootstrap
distributions of the estimator on the scale of \code{bhat}, centred and under
the complete null hypothesis), followed by the SILM additions
\code{boot.type}, \code{multiplier}, \code{robust}, \code{gaussian.stub}, \code{B.eff} (number of
usable bootstrap samples), \code{tstat} (the studentized statistics),
\code{boot.summary} (per bootstrap sample, the maximum, minimum and maximal
absolute value of the studentized bootstrap statistics, and the maximal
absolute value under the complete null hypothesis), \code{group.summary} and
\code{boot.index}.
}
\description{
Bootstraps the entire de-sparsified lasso (initial lasso fit included) to
obtain individual p-values, p-values adjusted for multiple testing with the
Westfall-Young max-T procedure, and bootstrap confidence intervals
(Dezeure, Bühlmann and Zhang, 2017). This is a port of
\code{boot.lasso.proj()} from the archived package 'hdi' (version 0.1-10) with
the same arguments and defaults.
}
\details{
The residual bootstrap (default) resamples the centred lasso residuals; the
wild bootstrap (\code{wild = TRUE}) multiplies them by independent standard
normal variables and is also valid for heteroscedastic errors. For every
bootstrap response the initial lasso is refitted (with the same tuning
method, or with the original lambda if \code{boot.shortcut = TRUE}) and the
de-sparsified lasso and its standard error are recomputed; the nodewise
residuals Z are computed once. Westfall-Young adjusted p-values use a
second bootstrap under the complete null hypothesis (all coefficients
zero), with the same resampled errors.

The paper recommends the robust standard error (\code{robust = TRUE}) in
practice, and the wild bootstrap under heteroscedastic errors.
}
\section{Compatibility with hdi 0.1-10}{

For the same data, arguments and random seed, \code{boot.lasso.proj()} returns
the same \code{pval}, \code{pval.corr}, \code{bhat}, \code{se}, \code{betahat}, \code{sigmahat}, \code{lambda}
and bootstrap distributions as \code{hdi::boot.lasso.proj()} run sequentially,
and leaves the random number generator in the same state; this is verified
against the archived hdi package (with the same version of glmnet). The
differences are:
\itemize{
\item The result has class \code{c("silm_boot_lasso_proj", "silm_proj")} instead of
\code{"hdi"}, and additional elements (see Value).
\item With \code{parallel = TRUE}, hdi drew the cross-validation folds of the
bootstrap refits in the worker processes, so its results were not
reproducible. SILM draws them in the main process in the same order as a
sequential run: parallel results are identical to sequential ones and to
hdi's sequential results.
\item Arguments are checked before the computations start; parallel execution
falls back to sequential on Windows.
}

Documented properties of hdi that are kept: a user-supplied \code{sigma} only
affects the standard errors of the original fit, not those of the
bootstrap fits (a warning is given); \code{boot.shortcut} has no effect with
\code{betainit = "scaled lasso"} (a warning is given) and uses glmnet's linear
interpolation along its lambda path; the robust standard error uses the
divisor n (Dezeure, Bühlmann and Zhang, 2017, Section 3.3.2); the
individual p-values are (2 c + 1) / (B + 1), where c is the smaller of the
two tail counts of the bootstrap distribution.
}

\section{SILM additions (not in hdi)}{

\itemize{
\item \code{boot.type = "wild"} with \code{multiplier = "mammen"}: the wild bootstrap
with Mammen's two-point multipliers (Section 4.1), which also match the
third moment of the errors.
\item \code{boot.type = "xyz"}: the xyz-paired bootstrap (Section 4.2), which
resamples rows of the design, the response and the nodewise residuals
after a correction that makes the bootstrap errors orthogonal to them.
It is defined with \code{robust = TRUE}; the paper reports that it is less
competitive than the Gaussian wild bootstrap.
\item Simultaneous confidence intervals (eq. 10) via
\code{confint(fit, type = "simultaneous")} and group tests via \code{\link[=groupTest]{groupTest()}}.
}

Validity (the paper's Theorems 1-3): the residual bootstrap is valid for
individual inference, and for simultaneous inference under homoscedastic
errors; the wild bootstrap with \code{robust = TRUE} is valid for individual and
simultaneous inference under heteroscedastic errors and is the paper's
preferred method in that case.
}

\examples{
set.seed(1)
x <- matrix(rnorm(50 * 15), 50, 15)
y <- x[, 1] + rnorm(50)
fit <- boot.lasso.proj(x, y, B = 50, boot.shortcut = TRUE, return.bootdist = TRUE)
fit
confint(fit, parm = 1:3)
}
\references{
Dezeure, R., Bühlmann, P. and Zhang, C.-H. (2017).
High-dimensional simultaneous inference with the bootstrap. \emph{TEST},
26, 685-719.

Dezeure, R., Bühlmann, P., Meier, L. and Meinshausen, N. (2015).
High-dimensional inference: confidence intervals, p-values and
R-software hdi. \emph{Statistical Science}, 30, 533-558.
}
\seealso{
\code{\link[=lasso.proj]{lasso.proj()}}, \code{\link[=confint.silm_proj]{confint.silm_proj()}}, \code{\link[=groupTest]{groupTest()}}, \code{\link[=Sim.CI]{Sim.CI()}}
}
