% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Sim_CI.R
\name{Sim.CI}
\alias{Sim.CI}
\title{Simultaneous Confidence Intervals}
\usage{
Sim.CI(
  X,
  Y,
  set,
  M = 500,
  alpha = 0.95,
  nodewise = c("ZnZ", "cv"),
  center = FALSE,
  Theta = NULL,
  parallel = FALSE,
  ncores = getOption("mc.cores", 2L)
)
}
\arguments{
\item{X}{Design matrix (n x p).}

\item{Y}{Response variable (a numeric vector of length n, or an n x 1
matrix).}

\item{set}{The set of variables of interest: column indices of \code{X}, or a
logical vector of length p.}

\item{M}{The number of bootstrap replications (default 500).}

\item{alpha}{The confidence level of the simultaneous intervals (default
0.95).}

\item{nodewise}{Tuning rule for the nodewise lasso that estimates
\eqn{\Theta} (the inverse of the Gram matrix) when \code{p > floor(n/2)}:
\code{"ZnZ"} (default) refines the lambda minimising 10-fold cross-validation
error pooled over all nodewise regressions with the rule of Zhang and
Zhang (2014); \code{"cv"} uses that cross-validated lambda itself, as
described in Zhang and Cheng (2017, Section 5). See the section
"Nodewise tuning" below.}

\item{center}{Logical. If \code{TRUE}, the columns of \code{X} and \code{Y} are centred
before the analysis. The default \code{FALSE} uses the data as given (as SILM
1.0.0 did) and warns when they do not appear to be centred.}

\item{Theta}{Optional precomputed \eqn{\hat\Theta} (a p x p matrix, e.g.
from \code{\link[=Theta.hat]{Theta.hat()}}) to avoid recomputing it. Note that supplying it skips
the random draw of the nodewise cross-validation folds, and that
\code{nodewise} is then not used: the tuning rule is the one \code{Theta} was
computed with. \code{\link[=Theta.hat]{Theta.hat()}} records it (attribute \code{"nodewise"}), and a
warning is given when \code{nodewise} is supplied explicitly and differs.}

\item{parallel, ncores}{Compute the nodewise regressions in parallel on
\code{ncores} cores (forking; not on Windows). This does not change the
results. With a BLAS library that uses OpenMP threads, forked workers can
occasionally stall; setting the environment variable \code{OMP_NUM_THREADS=1}
before starting R avoids this.}
}
\value{
A list with three components:
\item{de-biased Lasso}{the de-biased lasso estimates of \eqn{\beta_j},
\eqn{j \in} \code{set};}
\item{band.nst}{a 2 x |\code{set}| matrix whose rows \code{low.nst} and \code{up.nst}
are the lower and upper bounds of the intervals from the
non-studentized statistic;}
\item{band.st}{the same for the studentized statistic (rows \code{low.st},
\code{up.st}).}
}
\description{
Constructs simultaneous confidence intervals for \eqn{\beta_j},
\eqn{j \in} \code{set}, based on the de-biased lasso and a Gaussian multiplier
bootstrap of the maximum statistic (Zhang and Cheng, 2017, Section 2).
Both the non-studentized and the studentized versions are returned.
}
\details{
\strong{Note on \code{alpha}.} Unlike \code{\link[=ST]{ST()}} and \code{\link[=Step]{Step()}}, where \code{alpha} is the
significance level, here \code{alpha} is the \strong{confidence level} of the
simultaneous intervals (default 0.95, i.e. 95\% intervals). This is kept for
compatibility with SILM 1.0.0; a value below 0.5 triggers a warning.

See \code{\link[=SR]{SR()}} for the model assumptions (no intercept, centred data) and the
estimation of \eqn{\Theta}.
}
\section{Finite-sample behaviour}{

The intervals are asymptotically valid. In finite samples the de-biased
lasso keeps a bias term, \eqn{(I - \hat\Theta\hat\Sigma)(\hat\beta -
\beta)}{(I - Theta Sigma)(beta.hat - beta)}, that the bootstrap does not
reflect. It mainly affects the intervals for the non-zero coefficients, and
it grows with strong correlation, with signals of mixed signs and with the
number of signals. In the defaults study of the source repository
(\code{validation/calibration/defaults-results/REPORT.md}; n = 100, the paper's
designs, 200 replications each), the joint coverage of 95\% intervals over
the zero coefficients was at least 0.86 in every setting, but over S0 it
was 0.84-0.97 for s0 = 3 and 0.47-0.76 for s0 = 15 with positive signals
(with \code{nodewise = "cv"}: 0.80-0.96 and 0.31-0.60), and 0.45 for the signals
(1.5, -1, 2) in the Toeplitz design with p = 500 (\code{"cv"}: 0.21). Treat
intervals for sets that contain strong signals with caution unless s0 is
small.
}

\section{Nodewise tuning}{

SILM 1.0.0 obtained \eqn{\Theta} from an internal function of the 'hdi'
package. With hdi 0.1-6, current when SILM 1.0.0 was released (January
2019), the tuning parameter was the cross-validated lambda described in
Zhang and Cheng (2017, Section 5) (\code{"cv"}). hdi 0.1-7 (March 2019)
silently changed the default of that internal function to the Z&Z rule,
so from then until SILM was archived (July 2026) SILM installations
computed \code{nodewise = "ZnZ"} without saying so.

The default \code{"ZnZ"} of SILM 2.0.0 was chosen by a pre-registered study
that compared the two rules on the simulation designs of Zhang and Cheng
(2017) and on stress designs, under a decision rule fixed in advance
(calibration first, then power): over 20 \code{Sim.CI()}/\code{Step()} settings, 8 \code{SR()}
settings and 3 \code{ST()} settings (200 replications each), \code{"ZnZ"} halved the mean
calibration shortfall (coverage below 95\%, error rates above 5\%) from 0.100 to 0.053
(95\% bootstrap interval of the difference: -0.051 to -0.044). The joint coverage of
\code{Sim.CI()} for all p coefficients was higher or equal in all 20 settings (for example
0.77 with \code{"cv"} and 0.90 with \code{"ZnZ"} for signals (1.5, -1, 2) at p = 500), and exact
support recovery by \code{SR()} improved; the price is about 12\% wider intervals and
0.02-0.07 less power of \code{Step()} and \code{ST()} (see \code{validation/calibration/DEFAULTS.md}
and \code{validation/calibration/defaults-results/REPORT.md} in the source repository). With
the default, the results equal those of SILM 1.0.0 as
installed from 2019 to 2026 (with hdi 0.1-7 to 0.1-10; for \code{ST()} together
with \code{legacy = TRUE}). Use \code{nodewise = "cv"} for the tuning of the paper;
it reproduces SILM 1.0.0 with hdi 0.1-6. Both settings reproduce the
respective archived versions exactly (same numbers under the same random
seed).

\code{\link[=lasso.proj]{lasso.proj()}} and \code{\link[=boot.lasso.proj]{boot.lasso.proj()}} keep hdi's default for their
nodewise residuals (\code{do.ZnZ = FALSE}, the cross-validated lambda).
}

\examples{
## The function is intended for large n and p.
## Use small p here for illustration purpose only.
set.seed(1)
n <- 100
p <- 10
s0 <- 3
set <- 1:s0
Sigma <- matrix(NA, p, p)
for (i in 1:p) Sigma[i,] <- 0.9^(abs(i-(1:p)))
X <- matrix(rnorm(n*p), n, p)
X <- t(t(chol(Sigma))\%*\%t(X))
beta <- rep(0,p)
beta[1:s0] <- runif(s0,0,2)
Y <- X\%*\%beta+rt(n,4)/sqrt(2)
Sim.CI(X, Y, set)
}
\references{
Zhang, X., and Cheng, G. (2017) Simultaneous Inference for
High-dimensional Linear Models, \emph{Journal of the American Statistical
Association}, 112, 757-768.

Sun, T. and Zhang, C.-H. (2012). Scaled sparse linear regression.
\emph{Biometrika}, 99, 879-898.

Zhang, C.-H. and Zhang, S. S. (2014). Confidence intervals for low
dimensional parameters in high dimensional linear models. \emph{Journal
of the Royal Statistical Society, Series B}, 76, 217-242.
}
\seealso{
\code{\link[=SR]{SR()}}, \code{\link[=Step]{Step()}}, \code{\link[=ST]{ST()}}; \code{boot.lasso.proj()} for the bootstrapped
de-sparsified lasso of Dezeure, Bühlmann and Zhang (2017).
}
